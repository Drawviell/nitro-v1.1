import React from 'react';

    function MinimalTestComponent() {
      return (
        <div>
          <h1>This is a minimal test component.</h1>
        </div>
      );
    }

    export default MinimalTestComponent;
